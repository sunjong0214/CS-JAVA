c언어 포인터

int x =10 이라는 변수를 선언했다.
하지만 함수를 통해
x+3이라는 함수를 만든 후 
메인으로 돌아가 x를 출력하면 그대로인 10이 나온다
함수를 통할 때에는 x 그대로 가져오는 것이 아니라 x에 들어있는 10만 가져오는 것
하지만 포인터를 사용해 x의 주소값을 가져오면 문제 해결

포인터는 주소를 관리하는 개념

& - 주소
* - 선언시 or 값
이 두가지가 존재
위의 믄제를 해결하려면

int *p (선언)

p=&x (x주소를 넣을 때)

*p (x값)

